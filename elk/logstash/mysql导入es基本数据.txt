ES基本数据

SELECT pk, p_name, p_code, p_mobilenumber, p_storelist, p_paperid, 
CASE p_gender WHEN 8796112322651 THEN 'MAN' WHEN 8796112355419 THEN 'WOMAN' ELSE NULL END AS p_gender
FROM normalcustomer;

select pk, from vehicleinstance

SELECT pk, p_name, p_uid, p_oacoding, p_fullname
FROM ydstore;
